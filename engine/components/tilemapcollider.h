#ifndef TILEMAP_COLLIDER
#define TILEMAP_COLLIDER

/*
    Kinda ambiguous name, I know. By default, objects can only collide with tilemaps, not the other way around. So, tilemaps
    do not need a collision object. This component is for objects that collide with tilemaps.
*/

struct TilemapCollider
{
    //Tilemap* tilemap;
};

#endif